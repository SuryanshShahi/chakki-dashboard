export const localStorageKeys = {
  DEVICE_ID: "deviceId",
  REGISTERED_DEVICE_ID: "registeredDeviceId",
  DOWNLOAD_APP: "downloadApp",
};

export enum Status {
  PENDING = "pending",
  APPROVED = "approved",
}
